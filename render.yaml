services:
  - type: web
    name: interactive-story-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: DISCORD_TOKEN
        sync: false
      - key: BOT_TOKEN
        sync: false
      - key: DISCORD_BOT_TOKEN
        sync: false
